import java.net.*;
public class ReceberTCPThread extends Thread{
	/*private DataInputStream streamReceptor;
	private byte[] dados;
	public ReceberTCPThread(String nomeThread,byte[] buffer, DataInputStream stream){
		super(nomeThread);
		this.dados = buffer;
		this.streamReceptor = stream;
	}
	public void run(){
		try{
			Socket conexao =
			this.streamReceptor.receive(this.pacote);
		}
		catch(Exception ex){
			System.out.println("Erro ao receber");
		}
	}
	public byte[] getDados(){
		return this.pacote.getData();
	}
	public InetAddress getIPQuemEnviou(){
		return this.pacote.getAddress();
	}*/
}